import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oberj_merch.app',
  appName: 'oberj_merch',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
